import { createRequirementsLevels } from '../modules/level/level.util';

export const seedRequirementsLevels = () => {
  const requirementsLevels = createRequirementsLevels();

  console.log(requirementsLevels);
};
